#include "main.h"
/**
 * _abs(int) -prints the absolute value
 * Description: prints the absolute value
 *
 * Return: absolute value
 * 
 */
int _abs(int)
{
	int a,b;

	return (ab * ((ab > 0) - (ab < 0)));
}
