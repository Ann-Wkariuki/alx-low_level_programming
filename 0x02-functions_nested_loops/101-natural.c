#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;
	for(a < 1024)
	{
		if(a % 3=0, && a % 5=0)
	}
	printf( sum(a));

	return (0);
}
