#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints alphabets in lowercase
 *
 * Description: prints alphabets in lowercase
 *
 * Return: 0
 *
 */
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
	printf("\n")'
		return (0);
}
